# 🚀 本地开发指南

## 📋 前置要求

在开始之前，请确保你的电脑已安装：

1. **Node.js** (版本 14 或更高)
   - 下载地址：https://nodejs.org/
   - 推荐安装 LTS 版本
   - 验证安装：打开终端运行 `node --version`

2. **npm** (通常随 Node.js 一起安装)
   - 验证安装：`npm --version`

3. **Git** (用于克隆仓库)
   - 下载地址：https://git-scm.com/
   - 验证安装：`git --version`

## 🔧 本地开发步骤

### 步骤 1: 克隆仓库

打开终端（Terminal / CMD / PowerShell），运行：

```bash
# 克隆仓库到本地
git clone https://github.com/Davidsea-z/devportfolio.github.io.git

# 进入项目目录
cd devportfolio.github.io
```

### 步骤 2: 安装依赖

```bash
# 安装所有项目依赖
npm install
```

这个过程可能需要 2-5 分钟，取决于你的网络速度。

### 步骤 3: 启动开发服务器

```bash
# 启动开发服务器
npm start
```

执行后，你会看到：

```
Compiled successfully!

You can now view portfolio in the browser.

  Local:            http://localhost:3000/devportfolio.github.io
  On Your Network:  http://192.168.x.x:3000/devportfolio.github.io

Note that the development build is not optimized.
To create a production build, use npm run build.
```

### 步骤 4: 在浏览器中查看

开发服务器会自动打开浏览器，如果没有自动打开，请手动访问：

**http://localhost:3000/devportfolio.github.io**

## 🎨 你会看到的新功能

✅ **动态海浪背景**
- 平滑的缩放动画（20秒循环）
- 波浪叠加效果（8秒循环）
- 优雅的深色渐变遮罩

✅ **保留的原有功能**
- 粒子动画效果
- 响应式设计
- 所有项目展示

## 🔥 热重载 (Hot Reload)

开发服务器支持热重载！这意味着：

- 📝 修改代码后，浏览器会自动刷新
- ⚡ 无需手动重启服务器
- 🐛 错误会实时显示在浏览器控制台

## 🛠️ 开发流程

### 修改代码

1. 用你喜欢的编辑器打开项目文件夹
   - 推荐：VS Code, WebStorm, Sublime Text

2. 修改文件后，保存即可看到效果

3. 主要文件位置：
   ```
   src/
   ├── components/
   │   ├── Home/
   │   │   ├── Home.js              # 首页主组件
   │   │   ├── AnimatedBackground.js # 动画背景组件
   │   │   └── AnimatedBackground.css # 背景样式
   │   └── ...
   ├── Assets/
   │   ├── ocean-waves.jpg          # 海浪背景图
   │   └── ...
   └── style.css                    # 全局样式
   ```

### 调试技巧

1. **查看控制台**
   - 浏览器按 `F12` 打开开发者工具
   - 切换到 Console 标签查看日志

2. **查看网络请求**
   - 开发者工具 → Network 标签
   - 查看资源加载情况

3. **调整样式**
   - 开发者工具 → Elements 标签
   - 实时调整 CSS 看效果

## 📦 其他有用的命令

### 构建生产版本

```bash
# 创建优化的生产版本
npm run build
```

生成的文件在 `build/` 目录，可用于部署。

### 运行测试

```bash
# 运行测试套件
npm test
```

### 查看包依赖

```bash
# 查看项目依赖树
npm list

# 检查过期的包
npm outdated
```

### 更新依赖

```bash
# 更新所有依赖到最新版本（谨慎使用）
npm update

# 安全审计
npm audit

# 自动修复安全问题
npm audit fix
```

## 🐛 常见问题

### 问题 1: 端口 3000 已被占用

**错误信息：**
```
? Something is already running on port 3000.
Would you like to run the app on another port instead? (Y/n)
```

**解决方案：**
- 输入 `Y` 使用其他端口（如 3001）
- 或者停止占用 3000 端口的程序

**查找并关闭占用端口的程序：**

Windows:
```cmd
netstat -ano | findstr :3000
taskkill /PID <PID号> /F
```

Mac/Linux:
```bash
lsof -i :3000
kill -9 <PID号>
```

### 问题 2: npm install 失败

**解决方案：**

```bash
# 清除 npm 缓存
npm cache clean --force

# 删除 node_modules 和 package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install
```

### 问题 3: 编译错误

**解决方案：**

1. 检查 Node.js 版本是否符合要求：
   ```bash
   node --version  # 应该 >= 14.0.0
   ```

2. 清除缓存并重新编译：
   ```bash
   npm cache clean --force
   rm -rf node_modules build
   npm install
   npm start
   ```

### 问题 4: 图片或资源加载失败

**解决方案：**

1. 确保图片文件存在于正确路径
2. 检查文件名大小写（Linux/Mac 区分大小写）
3. 清除浏览器缓存（Ctrl+Shift+Del）

## 🔍 项目结构说明

```
devportfolio.github.io/
├── public/                      # 公共静态资源
│   ├── index.html              # HTML 模板
│   ├── ocean-waves.jpg         # 海浪背景图（备份）
│   └── ...
├── src/                        # 源代码
│   ├── Assets/                 # 资源文件
│   │   ├── ocean-waves.jpg     # 海浪背景图
│   │   └── ...
│   ├── components/             # React 组件
│   │   ├── Home/              # 首页组件
│   │   │   ├── Home.js        # 主组件
│   │   │   ├── Home2.js       # 第二部分
│   │   │   ├── Type.js        # 打字动画
│   │   │   ├── AnimatedBackground.js    # 🆕 动画背景
│   │   │   └── AnimatedBackground.css   # 🆕 背景样式
│   │   ├── About/             # 关于页面
│   │   ├── Projects/          # 项目页面
│   │   ├── Resume/            # 简历页面
│   │   ├── Navbar.js          # 导航栏
│   │   ├── Footer.js          # 页脚
│   │   └── Particle.js        # 粒子效果
│   ├── App.js                 # 主应用组件
│   ├── index.js               # 入口文件
│   ├── style.css              # 全局样式
│   └── ...
├── .github/                   # GitHub Actions 配置
│   └── workflows/
│       ├── deploy.yml         # 自动部署工作流
│       └── ci.yml             # CI 工作流
├── package.json               # 项目配置和依赖
├── README.md                  # 项目说明
└── DEPLOYMENT.md              # 部署指南
```

## 🎯 修改建议

### 自定义背景动画

编辑 `src/components/Home/AnimatedBackground.css`:

```css
/* 调整缩放动画时长 */
.animated-background-image {
  animation: slowZoom 20s ease-in-out infinite; /* 改为你想要的秒数 */
}

/* 调整波浪效果 */
.animated-background-overlay {
  animation: waveOverlay 8s ease-in-out infinite; /* 改为你想要的秒数 */
}
```

### 更换背景图片

1. 将新图片放到 `src/Assets/` 目录
2. 更新 `AnimatedBackground.css`:
   ```css
   .animated-background-image {
     background-image: url('../../Assets/你的图片.jpg');
   }
   ```

### 修改个人信息

编辑 `src/components/Home/Home2.js` 中的文本内容。

## 🚀 部署到生产环境

### 自动部署（已配置）

推送到 `main` 分支会自动触发 GitHub Actions 部署：

```bash
git add .
git commit -m "你的提交信息"
git push origin main
```

### 手动部署

```bash
# 构建并部署到 GitHub Pages
npm run deploy
```

## 💡 开发技巧

1. **使用浏览器扩展**
   - React Developer Tools
   - Redux DevTools (如果使用 Redux)

2. **代码格式化**
   - 安装 Prettier 扩展
   - 保存时自动格式化

3. **Git 提交规范**
   ```bash
   feat: 添加新功能
   fix: 修复 bug
   docs: 更新文档
   style: 代码格式调整
   refactor: 代码重构
   ```

## 📞 需要帮助？

- 查看 GitHub Issues
- 查看 React 官方文档：https://reactjs.org/
- 查看 Create React App 文档：https://create-react-app.dev/

---

祝你开发愉快！🎉
